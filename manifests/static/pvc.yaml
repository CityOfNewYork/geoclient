apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  # This name must be set as the value for PVC_NAME defined in base/pipeline.env
  name: pvc-geosupport-23c1-23-3-1
spec:
  accessModes:
  - ReadWriteOnce
  # This should be resizable according to:
  # https://learn.microsoft.com/en-us/azure/aks/azure-disk-csi
  # Verified by running:
  #   kubctl get sc managed-premium
  #   NAME              PROVISIONER          RECLAIMPOLICY   VOLUMEBINDINGMODE      ALLOWVOLUMEEXPANSION   AGE
  #   managed-premium   disk.csi.azure.com   Delete          WaitForFirstConsumer   true                   253d
  storageClassName: managed-premium
  resources:
    requests:
      storage: 20Gi
