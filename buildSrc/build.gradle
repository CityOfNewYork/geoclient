plugins {
    id 'groovy'
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        geosupportIntegrationTestPlugin {
            id = 'gov.nyc.doitt.gis.geoclient.gradle'
            implementationClass = 'gov.nyc.doitt.gis.geoclient.gradle.GeosupportIntegrationTestPlugin'
        }
    }
}
