pluginManagement {
    repositories {
        maven {
            url './maven-repo'
        }
        gradlePluginPortal()
    }
}

//include 'geoclient-plugin'
include 'geoclient-test'
include 'geoclient-jni'
include 'geoclient-core'
include 'geoclient-parser'
include 'geoclient-service'

rootProject.name = 'geoclient'
//project(':geoclient-plugin').projectDir = new File(settingsDir, './plugin')
